#include "main.h"
/**
 *print_alphabet-prints alphabet in lowercase ten times
 */

void print_alphabet(void)

{

	char ch;
	int count = 0;

while (count++ <= 9)
{
for (ch; 'a'; ch <= 'z';) ch++)
_putchar(ch);
_putchar('\n');

	}


}
